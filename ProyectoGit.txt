| Comando                        | Para qué sirve (resumen)                                                                    |
| ------------------------------ | ------------------------------------------------------------------------------------------- |
| `git config`                   | Configurar nombre/email de autor, alias personalizados, colores, editor, etc.               |
| `git init`                     | Crear un nuevo repositorio Git en la carpeta actual.                                        |
| `git clone [URL]`              | Copiar un repositorio remoto a tu máquina local (precarga `.git`).                          |
| `git status`                   | Mostrar archivos modificados, sin trackear o listos para commit.                            |
| `git add <archivo>…`           | Añadir cambios al “staging area” para el próximo commit.                                    |
| `git commit -m "…" `           | Guardar los cambios añadidos como una nueva instantánea de tu proyecto.                     |
| `git log`                      | Ver el historial de commits de la rama actual.                                              |
| `git diff`                     | Comparar cambios en archivos no añadidos (staged o unstaged).                               |
| `git checkout <rama>`          | Moverse entre ramas o cambiar de versión de un archivo (versión + HEAD).                    |
| `git reset <archivo>`          | Quitar un archivo del área de staging, sin alterar su contenido.                            |
| `git reset --hard <commit>`    | Volver al commit indicado, descartando todo lo no comiteado.                                |
| `git reflog`                   | Registro de movimientos recientes de HEAD (perfecto para "deshacer" resets).                |
| `git branch [rama]`            | Crear, listar o eliminar ramas locales (con `-d <rama>` borra una rama).                    |
| `git switch <rama>`            | Cambiar de rama sin usar el verbo antiguo ("checkout").                                     |
| `git merge <rama>`             | Fusionar otra rama dentro de la rama activa (integración de cambios).                       |
| `git stash`                    | Guardar temporalmente cambios no comiteados (útil antes de cambiar de rama).                |
| `git stash list / pop / drop`  | Ver, aplicar o descartar cambios guardados en la pila de `stash`.                           |
| `git tag [–a] <nombre>`        | Marcar un punto específico del historial como versión o referencia puntual.                 |
| `git fetch [remote]`           | Descargar historial remoto sin mezclarlo a la rama actual.                                  |
| `git pull`                     | Sincronizar el repositorio local con remoto (fetch + merge).                                |
| `git push [remote] [rama]`     | Enviar commits locales a la rama correspondiente en el remoto.                              |
| `git remote add/rename/origin` | Asociar o renombrar la URL de un repositorio remoto local.                                  |
| `git cherry-pick <SHA>`        | Aplicar un commit concreto (SHA) desde otra rama a la rama actual.                          |
| `git rebase <rama>`            | Mover automáticamente tus commits en la línea temporal de otra rama (más limpio que merge). |
| `git alias ...`                | Crear atajos personalizados para comandos complejos como `lg = log --oneline --graph`.      |

⚙️ Otras utilidades del curso:

* **`.gitignore`**: archivo que indica qué archivos o carpetas deben ser ignorados por Git.
* **Alias útiles**: permite atajos como `git co` para `git checkout`, `git cm` para `git commit`, etc. ([GitHub][2])
* También se explican comandos automáticos para flujos como `GitFlow`: `git flow init`, `git flow feature start`, etc. ([GitHub][2])

---

### 📋 Lista ampliada con comandos muy usados en Git

La guía oficial de GitHub (“Git cheat sheet”) incluye todos estos y otros comandos útiles más allá del curso, como `git rm`, `git mv`, `git show`, `git log --stat`, `git diff --staged`, entre otros.  ([education.github.com][3])

Estos comandos son especialmente relevantes:

* `git rm <archivo>`: eliminar archivo del repositorio (git + sistema de archivos).
* `git mv <origen> <destino>`: renombrar o mover archivo (registra el cambio).
* `git diff --staged`: compara cambios que ya están listos para ser comiteados.
* `git show <SHA>`: mostrar detalles de un commit específico.
* `git log branchB..branchA`: ver commits que están en una rama y no en otra.
* `git log --graph --decorate --oneline`: representación visual del árbol de commits.
* `git config --global core.excludesfile`: definir archivos ignorados a nivel usuario.



-------------------------------------------------------------------------------------------------
esto lo escribi para hacer cambios en la rama nobordes para despues unirla a la rama main
-------------------------------------------------------------------------------------------------
